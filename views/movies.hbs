<section class="sliders">

  <h1>Popular Movies</h1>
  <div class="shows-container-outer">
    {{#each moviesGroup.popular}}

      <div class="shows-container-inner">
        <a href="/profile"><img
            class="imgs"
            src="https://image.tmdb.org/t/p/w500{{this.poster_path}}"
            alt="image show {{title}}"
          /></a>
        <span>{{vote_average}}</span>
        <h3>{{this.title}}</h3>
      </div>
    {{/each}}
  </div>

</section>

<section class="sliders">

  <div class="lg-container">
    {{#each moviesGroup.topRated}}

      <div class="shows-container-inner">
        <a href="/profile"><img
            class="imgs"
            src="https://image.tmdb.org/t/p/w500{{this.poster_path}}"
            alt="image show {{title}}"
          /></a>
        <span>{{vote_average}}</span>
        <h3>{{this.title}}</h3>
      </div>
    {{/each}}
  </div>

</section>

<section class="sliders">

  <h1>Trending</h1>
  <div class="shows-container-outer">
    {{#each moviesGroup.kids}}

      <div class="shows-container-inner">
        <a href="/profile"><img
            class="imgs"
            src="https://image.tmdb.org/t/p/w500{{this.poster_path}}"
            alt="image show {{title}}"
          /></a>
        <span>{{vote_average}}</span>
        <h3>{{this.title}}</h3>
      </div>
    {{/each}}

  </div>

</section>